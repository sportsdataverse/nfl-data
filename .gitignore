__pycache__/
*.pyc
*.egg-info/
.venv/
python/.cache/
python/cfb/
python/nfl/
build/
dist/
.Rproj.user/
dev/

docs/models/_site/

# Build outputs / working data. These are NOT inputs: fd_model.ubj alone is
# 65 MiB, and a `git add -A -- python` swept 190 MiB of them into a commit
# on 2026-08-03 before this rule existed.
out/
**/out/
python/data/
.venv/

# Promoted model artifacts are NOT committed (recorded Track C step 6 decision:
# release tags + the sdv-py bundle are the durable stores; models/manifest.yaml).
models/*.ubj
models/*.json
.fingerprints.json
