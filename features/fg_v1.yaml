# Feature set v1 for `fg` — generated from the code constant;
# ordered equality with the constant is gated by tests/test_feature_sets.py.
# The constant is still the authority (migration step 2 of 3); inverting the
# dependency (code reads this file) is a recorded follow-up.
model: fg
version: 1
source_constant: model_training.decision_models.constants.FG_FEATURES
hyperparams_constant: model_training.decision_models.constants.FG_HYPERPARAMS
columns:
- yardline_100
- fg_roof
- era0
- era1
- era2
- era3
- era4
